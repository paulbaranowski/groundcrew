{
  "name": "clearance",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "packages/clearance/src",
  "tags": [],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["^build"],
      "inputs": ["production", "^production"],
      "outputs": ["{workspaceRoot}/dist/packages/clearance"],
      "options": {
        "command": "tsgo --build packages/clearance/tsconfig.lib.json",
        "forwardAllArgs": false
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "oxlint packages/clearance --deny-warnings",
        "forwardAllArgs": false
      },
      "outputs": []
    },
    "test": {
      "executor": "@nx/vitest:test",
      "options": {
        "configFile": "packages/clearance/vitest.config.ts"
      }
    }
  }
}
